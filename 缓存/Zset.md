# Zset
  - [ZSet底层](#zset底层)
  - [Redis的有序集合为什么用跳表，而不用平衡树、红黑树或者B+树？](#redis的有序集合为什么用跳表，而不用平衡树、红黑树或者b树？)
    - [平衡树VS跳表](#平衡树vs跳表)
    - [红黑树VS跳表](#红黑树vs跳表)
    - [B+树VS跳表](#b树-vs跳表)
  - [zset 延时队列怎么实现的](#zset延时队列怎么实现的)

## ZSet底层
zset是一个可以保证有序且元素唯一的集合，内部维护了一个score的参数来实现。所以它经常用于排行榜等需要进行统计排列的场景。

当有序集合对象同时满足ZSet **保存的键值对数量少于 128 个 同事 每个元素的长度小于 64 字节** ，使用 ziplist（压缩链表：有序集合使用紧挨在一起的压缩列表节点来保存，第一个节点保存member，第二个保存score。ziplist内的集合元素按score从小到大排序，score较小的排在表头位置）

如果不满足上述两个条件，那么使用 skiplist，跳表是一个多层次的链表，提供了一种平衡树的替代方案。在跳表中，一个元素可以出现在多个层次上，每个层次都是一个有序链表，实现简单，插入、删除、查找的复杂度均为O(logN)

## 跳表实现原理
**什么是跳表**
在普通链表的基础上，添加了“索引”，使得在查找元素时能跳过部分节点，快速定位。
**为什么使用跳表**
相比于红黑树，跳表的实现结构简单，代码易于后续的维护，插入删除操作更高效，适合范围查找和排序操作
**跳表的工作流程**
我的理解就是，和图书馆找书差不多，书架上有几层索引，最上层，每隔更多的书，放一个索引，中间层，每隔几本书，放一个索引，最底层，放所有书的索引，查找一本书的时候先在最高层找到大概的范围，再往下层精确定位，最终找到书籍

## Redis的有序集合为什么用跳表，而不用平衡树、红黑树或者B+树？
Redis使用跳表而不使用红黑树，是因为跳表的索引结构序列化和反序列化更加快速，方便持久化。
### 平衡树VS跳表
平衡树（AVL树）：一个严格的平衡二叉树，平衡条件必须满足（所有节点的左右子树高度差不超过1，及平衡因子范围为[-1,1]）。平衡树的插入、删除和查询的时间复杂度和跳表一样都是O（logn）
对于范围查询来说，它也可以通过中序遍历的方式达到跳表一样的效果，但是它的每一次插入或删除操作都需要保证整棵树左右节点的绝对平衡，只要不平衡就要通过旋转操作来保持平衡，这个过程是比较耗时的。

### 红黑树VS跳表
红黑树（Red Black Tree）也是一种自平衡的二叉查找树，它的查询性能略逊色于AVL树，但插入和删除的效率更高。红黑树的插入、删除和查询的时间复杂度和跳表一样都是O(logn)
红黑树是一个黑平衡树，即从任意节点到另外一个叶子叶子节点，它所经过的黑节点是一样的。当对它进行插入操作时，需要通过旋转和染色（红黑变换）来保证黑平衡。不过，相较于 AVL 树为了维持平衡的开销要小一些。

IO多路复用

### B+树VS跳表
B+树是一种常用的数据结构，具有以下特点：
1、多叉树结构：它是一棵多叉树，每个节点可以包含多个子节点，减小了树的高度，查询效率高。
2、存储效率高:其中非叶子节点存储多个key，叶子节点存储 value，使得每个节点更够存储更多的键，根据索引进行范围查询时查询效率更高。
3、平衡性：它是绝对的平衡，即树的各个分支高度相差不大，确保查询和插入时间复杂度为 O(log n) 。
4、顺序访问：叶子节点间通过链表指针相连，范围查询表现出色。
5、数据均匀分布：B+树插入时可能会导致数据重新分布，使得数据在整棵树分布更加均匀，保证范围查询和删除效率。

所以，B+树更适合作为数据库和文件系统中常用的索引结构之一，它的核心思想是通过可能少的 IO 定位到尽可能多的索引来获得查询数据。对于 Redis 这种内存数据库来说，它对这些并不感冒，因为 Redis 作为内存数据库它不可能存储大量的数据，所以对于索引不需要通过 B+树这种方式进行维护，只需按照概率进行随机维护即可，节约内存。而且使用跳表实现 zset 时相较前者来说更简单一些，在进行插入时只需通过索引将数据插入到链表中合适的位置再随机维护一定高度的索引即可，也不需要像 B+树那样插入时发现失衡时还需要对节点分裂与合并。

## zset 延时队列怎么实现的
* **原理**
ZSet 是 Redis 提供的一种数据结构，它的每个元素都关联着一个分数（score），并且元素会根据分数从小到大进行排序。利用 ZSet 的这个特性，我们可以将任务的执行时间作为分数，任务的唯一标识作为元素，将任务添加到 ZSet 中。之后，通过定时检查 ZSet 中分数小于等于当前时间的元素，将这些元素取出并执行相应的任务，从而实现延时队列的功能。