# excel
1、对列去重
=UNIQUE(选中列)


2、对多行多列数据去重
=UNIQUE(TOCOL(A2:C6,3))

如果输入0：不忽略特殊值
输入1：忽略空白单元格
输入2：忽略错误值
输入3：忽略空白单元格和错误值
第三参数：通过列扫描，FALSE，按行，TRUE按列，如果省略默认按行


3、核对两个excel数据是否一致
=IF(ISNA(MATCH(A2,E:E,0)),"否","是")
判断A2数据是否在E列中存在，注意写在A列旁边，插入数据之后需要修改位置
